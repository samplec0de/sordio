/// Точка входа скрипта главного мира. Отдельный файл, чтобы `level.ts`
/// оставался библиотекой без побочных эффектов и его можно было проверять
/// тестами, не запуская сам перехват.
import { install } from './level';

install();
